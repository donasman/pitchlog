# Cloudflare Pages 환경변수 설정 예시
# 실제 값은 Cloudflare Dashboard → Settings → Environment Variables 에서 설정

# Railway 백엔드 URL (배포 후 Railway에서 발급되는 URL로 교체)
NEXT_PUBLIC_API_URL=https://pitchlog-backend.up.railway.app
